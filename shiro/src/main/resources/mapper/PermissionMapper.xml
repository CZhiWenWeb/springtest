<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyx.shiro.mapper.PermissionMapper">
  <resultMap id="BaseResultMap" type="com.xyx.shiro.model.entity.Permission">
    <!--
      WARNING - @mbg.generated
    -->
    <result column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="per_code" jdbcType="VARCHAR" property="perCode" />
  </resultMap>

  <select id="findPermissionByRole" resultType="com.xyx.shiro.model.PermissionDto" parameterType="com.xyx.shiro.model.RoleDto">
    select
    p.id as id,
    p.name as name,
    p.per_code as perCode
    from
    role r
    left join role_permission rp on r.id=rp.role_id
    left join permission p on rp.permission_id=p.id
    <where>
      1=1
      <if test="name!=null and name!='' ">
        and r.name=#{name}
      </if>
    </where>
  </select>
</mapper>