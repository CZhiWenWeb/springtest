<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czw.browser.data.mapper.PermissionMapper">
    <resultMap id="BaseResultMap" type="com.czw.browser.data.model.entity.Permission">
        <!--
          WARNING - @mbg.generated
        -->
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="per_code" jdbcType="VARCHAR" property="perCode"/>
    </resultMap>

    <select id="selectPermissionByUserId" resultType="java.lang.String">
    select p.name from permission p
    left join role_permission rp on (p.id=rp.permission_id)
    left join role r on (rp.role_id=r.id)
    left join user_role ur on (r.id=ur.role_id)
    left join t_user u on (ur.user_id=u.id)
    where u.id=#{id}
  </select>
</mapper>