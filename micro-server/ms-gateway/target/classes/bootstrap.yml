spring:
  application:
    name: Ms-Gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: ms-gateway
        file-extension: yaml
