spring:
  application:
    name: Ms-Server-Test
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: ms-server-test
        file-extension: yaml
